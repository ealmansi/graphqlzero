{
  "name": "graphqlzero-root",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "lerna run test --stream",
    "dev": "concurrently --names 'now,lerna' 'now dev' 'lerna run dev --stream --parallel'",
    "prebuild": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start --stream --parallel"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "lerna": "^3.16.4",
    "now": "^16.2.0"
  }
}
