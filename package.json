{
  "name": "subocean-tiger-graphql",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "concurrently 'yarn build:watch' 'cd subocean-tiger-web && now dev'",
    "deploy": "cd subocean-tiger-web && now"
  },
  "dependencies": {
    "apollo-server-micro": "^2.9.0",
    "graphql": "^14.5.3",
    "isomorphic-fetch": "^2.2.1",
    "parse-link-header": "^1.0.1"
  },
  "devDependencies": {
    "@types/isomorphic-fetch": "^0.0.35",
    "@types/parse-link-header": "^1.0.0",
    "concurrently": "^4.1.2",
    "typescript": "^3.5.3"
  }
}
