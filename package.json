{
  "name": "subocean-tiger",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "postbuild": "yarn netlify",
    "dev": "lerna run dev --stream --parallel",
    "start": "lerna run start --stream --parallel",
    "netlify": "netlify-lambda build packages/subocean-tiger-graphql-server/dist/"
  },
  "devDependencies": {
    "bufferutil": "^4.0.1",
    "lerna": "^3.16.4",
    "utf-8-validate": "^5.0.2"
  },
  "dependencies": {
    "apollo-server": "^2.8.2",
    "netlify-lambda": "^1.6.3",
    "graphql": "^14.4.2"
  }
}
